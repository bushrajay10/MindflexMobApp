import 'package:flutter/material.dart';

class update_profile extends StatelessWidget{
  const update_profile ( { Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Scaffold(
      body: Center(child: Text('update_profile'),),
    );
  }
}